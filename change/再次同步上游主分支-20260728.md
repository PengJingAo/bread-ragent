# 再次同步上游主分支（2026-07-28）

## 同步范围

- 同步前本地 `main` 与 `origin/main` 一致，提交为 `25ab3e3`。
- 本地相对上游保留 12 个提交，上游相对本地新增 18 个提交。
- 本次以合并方式同步 `upstream/main` 的 `dc0d0013`，不变基、不强制推送。
- 安全备份分支为 `backup/main-before-upstream-sync-20260728`。

## 冲突处理

- 采用上游新的模型档位、超时预算和多知识库向量召回接口。
- 保留本地模型健康检查的调用凭证机制，避免半开状态下并发探测失控。
- 在新的回答来源、引用和溯源片段组装流程中，保留本地回答最大 token 配置。
- 保留关键词索引回灌、意图数量、回答 token 和关键词意图阈值配置。
- 提示词采用上游新的引用、链接和图片规则，同时保留本地按问题控制篇幅、禁止同类推断的约束。

## 校验结果

- `mvn -DskipTests compile -B`：通过，全部 Maven 模块编译成功。
- 模型档位、引用、PG 向量、多 Collection、查询向量复用和限流定向测试：通过。
- `DefaultContextFormatterTest` 与 `RAGPromptServiceTest`：修复 Windows CRLF 相关断言及上游模板/测试文本不一致后通过。
- `mvn test -B`：无需外部服务的测试通过；完整套件因本机未启动 Redis，以及部分集成测试需要数据库、Milvus 或模型服务而未能全绿。
- `mvn -pl mcp-server test -B`：通过，9 个测试通过，1 个真实外部 API 测试跳过。
- `npm run build`：通过，Vite 完成 4882 个模块的生产构建。
- `npm run lint`：未进入源码检查；现有 ESLint 8.57.1 无法解析 `plugin:react-refresh/recommended` 的新版 `name` 属性，属于上游工具链配置兼容问题。

## 额外兼容处理

- PG 向量检索继续使用 `metadata->>'collection_name'`，与本地写入、删除和关键词回灌的数据契约保持一致；同步调整上游新增测试的 SQL 断言。
- 提示词测试先归一化 CRLF/LF，避免 Windows 工作区产生平台相关失败。
